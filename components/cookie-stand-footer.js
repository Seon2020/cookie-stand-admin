export default function CookieStandFooter({ reports }) {
    return (
        <footer className="p-5 text-lg text-center bg-green-500">
            <p>{reports.length} Locations World Wide</p>
        </footer>
    )
}
